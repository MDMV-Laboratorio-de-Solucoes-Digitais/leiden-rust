# Proptest configuration for Leiden workspace (project convention)
# Local development: 1000 cases
# CI: overridden by PROPTEST_CASES=256

cases = 1000
max_shrink_iters = 200
failure_persistence = "SourceParallel"
