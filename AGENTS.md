# Agent Guidance

- Run graphify when graphs become stale: regenerate the codebase knowledge graph (`graphify-out/`) after significant structural or cross-crate API changes, before relying on `/graphify` queries for architectural context (see Constitution Principle VIII).
