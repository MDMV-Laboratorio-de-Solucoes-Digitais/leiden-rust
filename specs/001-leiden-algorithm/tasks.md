# Tasks: Leiden Algorithm in Rust

**Input**: Design documents from `/home/luis/development/leiden/specs/001-leiden-algorithm/`
**Prerequisites**: plan.md (required), spec.md (required for user stories), research.md, data-model.md, contracts/, quickstart.md
**Tests**: **Required** — Constitution §V (Test-First NON-NEGOTIABLE). Every implementation task is preceded by a failing-test task.
**Organization**: Tasks grouped by user story (P1, P2, P3) plus TUI phase. Each story is independently implementable and testable.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies on incomplete tasks)
- **[Story]**: Which user story this task belongs to (US1, US2, US3, TUI)
- **TDD marker**: `[T]` = failing-test task (write first); `[I]` = implementation task (after test fails); `[V]` = verification task (test passes + acceptance scenario check)
- Include exact file paths in descriptions

## Path Conventions

- Workspace root: `leiden/` (per `plan.md §Project Structure`)
- Library crate: `leiden/crates/leiden/`
- CLI binary: `leiden/crates/leiden-cli/`
- TUI binary: `leiden/crates/leiden-tui/`
- Fixtures: `leiden/fixtures/`
- Workspace manifests: `leiden/Cargo.toml`, `leiden/rust-toolchain.toml`

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Project initialization, tooling, lint profile, fixture corpus, dependency manifest.

- [x] T001 Create Cargo workspace at `leiden/` with empty `Cargo.toml` and `[workspace]` table; create `leiden/rust-toolchain.toml` pinning stable Rust, edition 2024. Pin to a stable channel that satisfies the floor imposed by `ratatui 0.30.2`'s `rust_version = "1.88.0"` (see `plan.md §Constraints (MSRV floor CRITICAL)`); e.g., `toolchain = "1.88.0"` or newer. Per Constitution Additional Constraints 'Language & edition', an MSRV bump requires a PATCH amendment to `.specify/memory/constitution.md`.
- [x] T002 Create workspace subcrate directories `leiden/crates/leiden/src/`, `leiden/crates/leiden-cli/src/`, `leiden/crates/leiden-tui/src/` (empty `lib.rs`/`main.rs` stubs)
- [x] T003 Copy `[workspace.lints.rust]` and `[workspace.lints.clippy]` blocks verbatim from `rust-code-rigor.md` into `leiden/Cargo.toml [workspace.lints]`
- [x] T004 Add pinned dependencies via `cargo add` in `leiden/crates/leiden/Cargo.toml`: `thiserror`, `tracing`, `tracing-subscriber` (dev), `proptest` (dev), `criterion` (dev), `tracing-test` (dev). `rand` is **not** added in v1; see `plan.md` Primary Dependencies.
- [x] T005 [P] Add pinned dependencies in `leiden/crates/leiden-cli/Cargo.toml`: `clap` (derive), `serde`, `serde_json`, `tracing-subscriber`, `thiserror`
- [x] T006 [P] Add pinned dependencies in `leiden/crates/leiden-tui/Cargo.toml`: `ratatui = "0.30.2"`, `crossterm`, `color-eyre`, `tracing-subscriber`, `thiserror`
- [x] T007 [P] Create `leiden/fixtures/` directory and add fixture files from `quickstart.md §2`: `single_node.edg`, `empty.edg`, `two_cliques.edg`, `karate.edg`, `ring_of_cliques.edg`, `star.edg`, `path.edg`, `lfr_small.edg`
- [x] T007a [P] Create `leiden/fixtures/disconnected_two_triangles.edg` (two disjoint K3 cliques, 6 nodes, 6 weighted edges) and `leiden/fixtures/disconnected_two_triangles.expected.json` per `quickstart.md §2` conventions
- [x] T007b [P] Create `leiden/fixtures/barabasi_albert_small.edg` (Barabási–Albert preferential attachment, n=50, m=2, deterministic seed) + `.expected.json` for `spec.md SC-002` cardinality
- [x] T007c [P] Create `leiden/fixtures/gnp_sparse.edg` (Erdős–Rényi G(n,p), n=40, p=0.05, deterministic seed) + `.expected.json` for `spec.md SC-002` cardinality (now ≥ 10 curated graphs per SC-002)
- [x] T007d [P] [T] Failing test asserting SC-002 fixture-cardinality invariant at Phase 1 (closes `A10` from `$speckit-analyze`): walk `leiden/fixtures/` at test time, exclude `_classification.json`, parse every `*.expected.json` file, and assert `fixture_count >= 10` BEFORE any user-story work begins. This is the **write-time** counterpart to T116b (Phase 7 cardinality assertion); together they ensure that adding fewer than 10 fixtures in T007–T007c fails immediately at Phase 1 rather than being caught only at the final pre-merge gate. **Dependency**: this test depends on T007c (the 10th fixture); T007d itself MUST be marked `[T]` and added to a Phase-1 failing-test assertion that runs as part of T011's verification step so a future PR that drops a fixture file fails the build at Phase 1, not Phase 7. In `leiden/crates/leiden/tests/sc002_fixture_cardinality_phase1.rs` (named `sc002_curated_suite_has_at_least_10_fixtures_at_phase_1`); the test body is identical to T116b but its `tests/` location and `[P] [T]` markers disambiguate the two enforcement points. **Rationale**: a maintainer who adds 9 fixtures could pass every Phase 3 US1 test (which exercises per-fixture partitions, not cardinality); without T007d, the SC-002 ≥10 contract is silent until T116b runs in Phase 7, which is late and expensive to debug.
- [x] T008 [P] Author `leiden/fixtures/*.expected.json` (reference partitions) for each fixture under γ=1.0, seed=0, per `quickstart.md §2`
- [x] T008a [P] Author `leiden/fixtures/_classification.json` mapping each fixture filename to `{unique: bool, gamma: 1.0, seed: 0, reference_partition_hash: <u64>}` per `quickstart.md §2` (closes `E4` from `$speckit-analyze` — the uniqueness-vs-non-unique distinction is **informational only in v1** per the rewritten `spec.md SC-002` post-2026-08-31 reconciliation: the v1 algorithm is deterministic, so 100% match is the only meaningful target and the `unique: false` field does NOT gate a "≥ 90% path" — that path is reserved for a future stochastic variant and is not a v1 contract). T116a consumes this metadata for documentation/reporting only.
- [x] T009 Author `leiden/README.md` documenting MSRV, scope, citation (Traag 2019), and per `Constitution Additional Constraints` — README's MSRV statement MUST reflect the floor imposed by ratatui 0.30.2's `rust_version = "1.88.0"` (closes `C1` from `$speckit-analyze`; per Constitution Additional Constraints "Language & edition").
- [x] T010 [P] Configure `leiden/.gitignore` for `target/`, `Cargo.lock` workspace exclusions, `*.snap.new`
- [x] T011 Verify `cargo check --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo fmt --check` all pass on the stub-crate workspace (Phase 1 stubs: `leiden/crates/*/src/lib.rs` and `main.rs` files exist but are empty) — the lint profile copy from `rust-code-rigor.md` is the gate
- [x] T011a [P] Add a workspace-wide panic-trace grep test that runs `grep -E "panicked at|thread 'main'"` across all test output under `cargo test --workspace` and asserts zero matches (Constitution §III + FR-008). Implementation in `leiden/crates/leiden/tests/workspace_panic_trace_grep.rs` (named `workspace_no_panic_traces_under_any_test`); may be a shell-driven integration test. This complements the Phase 5 T095a CLI-scoped assertion by covering the library from Phase 1 onwards.
- [x] T011b [P] Add a pre-commit / CI lint that flags any `pub` item or function body in `leiden/crates/leiden/src/{local_moving,refinement,aggregation,quality,orchestrator}/` that lacks either a `// ref: Traag 2019 §X.Y` citation comment (regex `\bTraag\s+2019\s+§\d+(\.\d+)?\b`) or a `// leiden-deviation:` marker (per FR-009 + T138a audit). Implementation: a `compile_fail` doctest in `leiden/crates/leiden/src/lib.rs` plus a CI test in `leiden/crates/leiden/tests/fr009_no_uncited_deviations.rs` (named `fr009_no_uncited_deviations`). Catches drift at write time; T138a remains the final pre-merge audit.

**Checkpoint**: Workspace builds clean with strict lint profile; ready for foundational types.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Core domain types that ALL user stories depend on. No user story work begins until this phase is complete.

**⚠️ CRITICAL**: Blocks Phase 3 (US1), Phase 4 (US2), Phase 5 (US3), Phase 6 (TUI).

### Tests for foundational types (write FIRST, observe FAIL) ⚠️

- [x] T012 [P] [T] Unit test: `NodeId` trait blanket impl covers `String`, `u32`, `Uuid` in `leiden/crates/leiden/src/graph/node_id.rs` (`#[cfg(test)] mod tests`)
- [x] T013 [P] [T] Unit test: `LeidenError` `Display` output matches expected `thiserror` formats in `leiden/crates/leiden/src/error.rs`
- [x] T014 [P] [T] Unit test: `LeidenParameters::validate` rejects `gamma <= 0` with `InvalidGamma`, rejects `iteration_cap < 1` with `InvalidIterationCap`, accepts defaults in `leiden/crates/leiden/src/params.rs`
- [x] T015 [P] [T] Unit test: `LeidenEvent` variants `Debug` + `Clone` derive, exhaustive match in `leiden/crates/leiden/src/events.rs`
- [x] T015a [P] [T] Failing test: per-`LeidenEvent` variant structured-field assertion — for each variant in `data-model.md §1.8`, construct the variant, dispatch it through a `tracing_subscriber::fmt::TestWriter`, and assert the emitted `tracing` line contains every named field listed in `data-model.md §1.8` (e.g., for `LocalMovingProgress`, assert the substrings `iteration=` and `moved_nodes=` appear). Covers Constitution §VI "structured fields, no stringly-typed log lines without context" at the type layer. In `leiden/crates/leiden/src/events.rs` unit tests (named `leidenevent_variants_emit_named_fields`).

### Implementation for foundational types

- [x] T016 [I] Implement `NodeId` trait with supertraits `Hash + Eq + Clone + Ord` (blanket `impl<T: Hash + Eq + Clone + Ord> NodeId for T`) with `///` docs in `leiden/crates/leiden/src/graph/node_id.rs` (depends on T012)
- [x] T017 [I] Implement `LeidenError` thiserror enum per `data-model.md §1.11` (variants `Graph`, `InvalidWeight`, `SelfLoop`, `DanglingNode`, `InvalidGamma`, `InvalidIterationCap`, `EmptyGraph`) with `///` docs in `leiden/crates/leiden/src/error.rs` (depends on T013)
- [x] T018 [I] Implement `LeidenParameters` struct (`gamma: f64`, `seed: Option<u64>`, `iteration_cap: u32`) with `Default` returning `gamma = 1.0, seed = None, iteration_cap = 10` + `validate()` per `data-model.md §1.5` and `spec.md §Clarifications 2026-08-30 (seed field)` in `leiden/crates/leiden/src/params.rs` (depends on T014)
- [x] T019 [I] Implement `LeidenEvent` enum + `Phase` enum per `data-model.md §1.8` with `#[derive(Debug, Clone)]` and `///` docs in `leiden/crates/leiden/src/events.rs` (depends on T015)
- [ ] T019a [P] Create `specs/001-leiden-algorithm/checklists/observability.md` with placeholder sections CHK073–CHK087 (one row per `LeidenEvent` variant from `data-model.md §1.8`; columns: `Variant`, `Required Fields`, `Assertion Strategy`). Populate rows for `IterationStarted`, `LocalMovingProgress`, `LocalMovingDelta`, `RefinementMerged`, `Aggregation`, `QualityComputed`, `IterationFinished`, `Terminated`, and `Throttled` with the field list from `data-model.md §1.8`. T126 consumes this checklist for per-variant per-field assertions.
- [x] T020 [I] Re-export public types from `leiden/crates/leiden/src/lib.rs` per `library-api.md §1`: `LeidenError`, `CsrGraph`, `Edge`, `NodeId`, `Leiden`, `RunResult`, `TerminationReason`, `Partition`, `Modularity`, `QualityFunction`, `MoveComponents`, `LeidenParameters`, `LeidenEvent`, `Phase`
- [x] T021 [V] Verify `cargo test --workspace` passes all foundational unit tests; verify `cargo doc --workspace --no-deps` builds (Constitution §IV `missing_docs = deny`)

**Checkpoint**: Foundation ready — all public types compile, all unit tests green, docs build. User story work can begin.

---

## Phase 3: User Story 1 — Detect Communities on a Small Graph (Priority: P1) 🎯 MVP

**Goal**: User provides a small weighted graph and resolution parameter; receives a partition of nodes into disjoint, internally-connected communities.

**Independent Test**: Run `cargo test -p leiden two_cliques_yields_two_communities` on `fixtures/two_cliques.edg` — assert two distinct communities, each internally connected. Also: `single_node_returns_one_community`, `empty_graph_returns_empty_partition` (`quickstart.md §3`).

### Tests for User Story 1 (write FIRST, observe FAIL) ⚠️

- [ ] T022 [P] [T] [US1] Failing test: `CsrGraph::from_edges` accepts a 4-clique + 5-clique fixture and produces correct CSR offsets, neighbour list, weights, and node_count/edge_count/total_weight in `leiden/crates/leiden/tests/graph_construct.rs`
- [ ] T023 [P] [T] [US1] Failing test: `CsrGraph::from_edges` rejects negative weight with `LeidenError::InvalidWeight { line, value }` in `leiden/crates/leiden/tests/graph_construct.rs`
- [ ] T024 [P] [T] [US1] Failing test: `CsrGraph::from_edges` rejects self-loop with `LeidenError::SelfLoop { line: None, node }` (library path emits `line: None` because the `IntoIterator<Item = Edge<Id>>` API has no source-line context; the CLI emits `line: Some(N)` — see T081) in `leiden/crates/leiden/tests/graph_construct.rs`
- [ ] T024a [P] [T] [US1] Failing test asserting `SelfLoop { line: None, node }.node` payload (closes part of `U1` from `$speckit-analyze`, post C1 contract resolution): construct a self-loop edge on a graph with three nodes `{a, b, c}`; assert the returned `LeidenError::SelfLoop { line, node }` carries `node == <id of self-looped node rendered as String>` and `line == None` (library path has no line context per `spec.md` FR-008 and `data-model.md §1.11`). The CLI counterpart asserts `line == Some(N)` (T081). In `leiden/crates/leiden/tests/graph_construct.rs` (named `selfloop_error_payload_matches_input_node`).
- [ ] T025 [P] [T] [US1] Failing test: `CsrGraph::from_edges` rejects empty input with `LeidenError::EmptyGraph` in `leiden/crates/leiden/tests/graph_construct.rs`
- [ ] T026 [P] [T] [US1] Failing test: `Partition::singletons(n)` produces n communities, `community_of(node)` returns correct id in `leiden/crates/leiden/src/partition/mod.rs` unit tests
- [ ] T027 [P] [T] [US1] Failing test: `Partition::move_node` updates `sigma_in` and `sigma_tot` incrementally per `data-model.md §1.4` in `leiden/crates/leiden/src/partition/mod.rs`
- [ ] T028 [P] [T] [US1] Failing test: `Partition::is_refinement_of` returns true when self's communities are subsets of other's communities in `leiden/crates/leiden/src/partition/mod.rs`
- [ ] T029 [P] [T] [US1] Failing test: `Modularity::total_quality` returns `0.0` for empty graph (no NaN, no panic) and matches reference value on `fixtures/two_cliques.edg` in `leiden/crates/leiden/src/quality/modularity.rs`
- [ ] T030 [P] [T] [US1] Failing test: `Modularity::delta_move` matches Traag 2019 Eq. (A5) hand-computed ΔQ on a 3-node triangle in `leiden/crates/leiden/src/quality/modularity.rs`
- [ ] T031 [P] [T] [US1] Failing test: `MoveComponents` struct fields populated correctly per `data-model.md §1.7` in `leiden/crates/leiden/src/quality/modularity.rs`
- [ ] T032 [P] [T] [US1] Failing test: `Leiden::run` on `fixtures/two_cliques.edg` returns partition with two communities, each internally connected, modularity > 0.4 in `leiden/crates/leiden/tests/us1_two_cliques.rs` (named `two_cliques_yields_two_communities`)
- [ ] T032a [P] [T] [US1] Failing test asserting FR-002 directly (closes part of `E1` from `$speckit-analyze`): `Leiden::run` on `fixtures/two_cliques.edg` produces a partition whose induced subgraphs (per community) are each connected — assert `is_connected(community)` returns true for every community in the returned partition; covers the headline correctness invariant at US1 acceptance time, prior to Phase 7's `proptest_communities_connected` (T120). **Refinement-phase invariant sub-assertion**: instrument the refinement phase (T044) to expose intermediate partitions; assert each intermediate partition's communities are also internally connected (this asserts the Leiden invariant *during* the algorithm, complementing the post-hoc T120 statistical coverage). In `leiden/crates/leiden/tests/us1_two_cliques.rs` (named `two_cliques_communities_are_internally_connected`).
- [ ] T033 [P] [T] [US1] Failing test: `Leiden::run` on `fixtures/single_node.edg` returns one community, no error in `leiden/crates/leiden/tests/us1_single_node.rs` (named `single_node_returns_one_community`)
- [ ] T033a [P] [T] [US1] Failing test: `Leiden::run` on a single-node-zero-edge graph (1 node, 0 edges) returns exactly one community containing that node and `TerminationReason::Converged` (NOT `DegenerateInput`); per `spec.md` Edge Case "Zero-edge connected components" (closes `T4` + `F2` from `$speckit-analyze`). The graph has 1 node and 0 edges; total edge weight is 0 but total node count is 1, so `DegenerateInput` (zero total nodes OR zero total edges per FR-003a) does NOT trigger. In `leiden/crates/leiden/tests/us1_single_node.rs` (named `single_node_zero_edges_is_not_degenerate`).
- [ ] T034 [P] [T] [US1] Failing test: `Leiden::run` on `fixtures/empty.edg` returns empty partition with `TerminationReason::DegenerateInput` in `leiden/crates/leiden/tests/us1_empty.rs` (named `empty_graph_returns_empty_partition`)
- [ ] T035 [P] [T] [US1] Failing test: `Leiden::run` on `fixtures/karate.edg` returns partition matching `karate.expected.json` exactly (post-2026-08-31 SC-002 reconciliation: v1 algorithm is fully deterministic per FR-004 + Clarifications 2026-08-30, so no tie-breaking tolerance applies — the reference partition is bit-exact under the documented lowest-id tie-break rule) in `leiden/crates/leiden/tests/us1_fixture_suite.rs` (named `fixture_suite_matches_reference`)
- [ ] T035a [P] [T] [US1] Failing test: `Modularity::total_quality` matches Traag 2019 Eq. (1) on a hand-computed 4-clique+bridge fixture (Q=0.5*((2/3) - (8/3)^2/(2*3)) = 0.1666...) within `f64::EPSILON` in `leiden/crates/leiden/tests/us1_traag_fidelity.rs` (named `traag_eq1_modularity_matches`)
- [ ] T035b [P] [T] [US1] Failing test: `Modularity::delta_move` matches Traag 2019 Eq. (A5) on a 4-node path where the hand-computed ΔQ is known (±0.001) in `leiden/crates/leiden/tests/us1_traag_fidelity.rs` (named `traag_eq_a5_delta_move_matches`)
- [ ] T035c [P] [T] [US1] Failing test: Refinement merge predicate matches Traag 2019 §3: `Σ_in(D, C\D) ≥ k_D · (k_C − k_D)` — fixture with two subsets A,B inside community C where the inequality decides accept/reject; assert the boundary case in `leiden/crates/leiden/tests/us1_traag_fidelity.rs` (named `traag_refinement_predicate_matches`)
- [ ] T035d [P] [T] [US1] Failing test: Aggregation step matches Traag 2019 Algorithm A.2 lines 44–48 — given a refined partition, the aggregate graph's node/edge/weight counts match a hand-computed reference in `leiden/crates/leiden/tests/us1_traag_fidelity.rs` (named `traag_aggregation_matches_a2`)
- [ ] T035g [P] [T] [US1] Failing test gating Traag citation discipline on `us1_traag_fidelity.rs` (closes `C2` from `$speckit-analyze`): a CI test that opens `crates/leiden/tests/us1_traag_fidelity.rs`, scans every `const FIXTURE_*` / `fn hand_*` / named fixture / inline hand-computed value referenced by T035a / T035b / T035c / T035d, and asserts each one carries an adjacent `// ref: Traag 2019 §X.Y` comment with the citation regex `\bTraag\s+2019\s+§\d+(\.\d+)?\b` AND a rationale comment ≥ 20 non-whitespace characters immediately above (parallels the T138a audit but scoped to the fidelity-test fixtures only, so the contract is enforced *before* the audit catches drift). Test name: `us1_traag_fidelity_fixtures_carry_citations`. Rationale: hand-computed reference values that themselves lack a citation to Traag 2019 are unanchored; a future refactor could silently change the value and the test would still pass against an unverified hand-computation. This task makes the citation discipline mandatory at the fixture-definition level.
- [ ] T035e [P] [T] [US1] Failing test: `Leiden::run` on `fixtures/disconnected_two_triangles.edg` (two disconnected 3-cliques, 6 nodes total) returns ≥ 2 communities; no community contains nodes from both triangles; and **every weakly-connected component of the input graph receives at least one community** (per-component invariant per `spec.md` Edge Cases "Disconnected graph" and Edge Case "Zero-edge connected components": an isolated node or a component with no internal edges is one community, NOT `DegenerateInput`) in `leiden/crates/leiden/tests/us1_disconnected.rs` (named `disconnected_components_separated`)
- [ ] T035f [P] [T] [US1] Failing test: `Leiden::run` on a fixture containing one zero-weight edge returns a valid partition without `LeidenError::InvalidWeight` (asserts `spec.md` Edge Cases "Numerical extremes" — zero-weight edges must not produce `NaN`/`±∞` and must not be rejected as invalid); construct a small K3 with one of its three edges assigned `weight = 0.0`; assert `result.quality.is_finite()` and the partition is non-empty in `leiden/crates/leiden/tests/us1_zero_weight.rs` (named `zero_weight_edge_does_not_panic`)

### Implementation for User Story 1

- [ ] T036 [I] [US1] Implement `Edge<Id>` struct per `data-model.md §1.2` in `leiden/crates/leiden/src/graph/edge.rs`
- [ ] T037 [I] [US1] Implement `CsrGraph<Id>` struct fields + accessors (`node_count`, `edge_count`, `total_weight`, `degree_of`, `neighbours_of`, `weights_of`) per `data-model.md §1.3` and `library-api.md §3` in `leiden/crates/leiden/src/graph/csr_graph.rs` (depends on T036)
- [ ] T038 [I] [US1] Implement `CsrGraph::from_edges` with validation (negative weight, self-loop, NaN/∞, empty input) and deterministic first-seen-order numbering in `leiden/crates/leiden/src/graph/csr_graph.rs` (depends on T022–T025)
- [ ] T038a [P] [T] [US1] Failing test asserting parallel-edge summing (closes part of `U1` from `$speckit-analyze`): construct a graph with two parallel edges between nodes `a` and `b` (weights `0.3` and `0.7`, first-seen order); assert the resulting CSR stores a single adjacency entry `(b, weight = 1.0)` (sum, per `spec.md` Edge Cases "Parallel edges") in `leiden/crates/leiden/tests/graph_construct.rs` (named `parallel_edges_summarised_into_single_csr_entry`).
- [ ] T039 [I] [US1] Implement `Partition` struct + methods (`singletons`, `community_count`, `community_of`, `move_node`, `renumber`, `is_refinement_of`) per `data-model.md §1.4` in `leiden/crates/leiden/src/partition/mod.rs` (depends on T026–T028)
- [ ] T040 [I] [US1] Implement `MoveComponents` struct per `data-model.md §1.7` in `leiden/crates/leiden/src/quality/modularity.rs` (depends on T031)
- [ ] T041 [I] [US1] Implement `Modularity::total_quality` (Traag 2019 Eq. 1) with `m == 0` guard returning `0.0` per `research.md §1.4` in `leiden/crates/leiden/src/quality/modularity.rs` (depends on T029)
- [ ] T042 [I] [US1] Implement `Modularity::delta_move` (Traag 2019 Eq. A5) with `+ kᵢ` self-loop compensation per `research.md §1.3` in `leiden/crates/leiden/src/quality/modularity.rs` (depends on T030, T040)
- [ ] T043 [I] [US1] Implement local-moving phase (greedy ΔQ maximization with `MoveComponents` caching) per `research.md §1.5`; correctness anchored by `traag_eq_a5_delta_move_matches` (T035b) in `leiden/crates/leiden/src/local_moving/mod.rs`
- [ ] T044 [I] [US1] Implement refinement phase (deterministic `MergeNodesSubset` per Traag A.2 line 37 predicate `Σ_in(D, C\D) ≥ k_D · (k_C − k_D)`); correctness anchored by `traag_refinement_predicate_matches` (T035c) per `research.md §1.3` in `leiden/crates/leiden/src/refinement/mod.rs`
- [ ] T045 [I] [US1] Implement aggregation phase (build aggregate graph from refined partition per Traag A.2 lines 44–48); correctness anchored by `traag_aggregation_matches_a2` (T035d) in `leiden/crates/leiden/src/aggregation/mod.rs`
- [ ] T046 [I] [US1] Implement `Leiden` orchestrator struct + `Leiden::new()`, `Leiden::run` per `library-api.md §2` driving the outer loop with termination check per `research.md §1.5` in `leiden/crates/leiden/src/orchestrator/mod.rs` (depends on T043–T045)
- [ ] T047a [P] [T] [US1] Failing test: `RunResult<Id>` exposes `.partition` (`Vec<(Id, u32)>` sorted by `Id`), `.quality` (finite `f64`), `.iterations` (`u32`), and `.termination_reason` (matches `TerminationReason` variants) as direct public fields per `library-api.md §7` and `spec.md` FR-005 in `leiden/crates/leiden/src/orchestrator/result.rs` unit tests. **Per-`TerminationReason` `iterations` assertions** (closes `A2` from `$speckit-analyze`): `TerminationReason::Converged → result.iterations == number of completed outer-loop passes`, `TerminationReason::DegenerateInput → result.iterations == 0`, `TerminationReason::IterationCap → result.iterations == cap` (per FR-003a clarification 2026-08-31). The test name is `runresult_iterations_semantics_per_termination_reason`.
- [ ] T047b [I] [US1] Implement `RunResult<Id>` + `TerminationReason` enum + accessors per `data-model.md §1.9, §1.10` and FR-005 in `leiden/crates/leiden/src/orchestrator/result.rs` (depends on T047a). **Includes (closes I1)**: `RunResult.seed: Option<u64>` field that round-trips the value supplied via `LeidenParameters::seed` verbatim (`None` in → `None` out, `Some(s)` in → `Some(s)` out) per `spec.md` Clarifications 2026-08-30 (seed field).
- [ ] T047c [P] [T] [US1] Failing test asserting FR-012 `RunResult.threading` field (closes `I1` from `$speckit-analyze`): assert `result.threading: ThreadingPolicy` is a direct public field on `RunResult<Id>` and that **every** code path — `TerminationReason::Converged` (e.g. `fixtures/two_cliques.edg`), `TerminationReason::DegenerateInput` (e.g. `fixtures/empty.edg`), and `TerminationReason::IterationCap` (e.g. `fixtures/path.edg` with `iteration_cap = 2`) — produces `result.threading == ThreadingPolicy::SingleThreaded` per `spec.md` FR-012 ("v1 always produces `ThreadingPolicy::SingleThreaded`"). The default `Leiden::default()` (no `with_threads` call) MUST also yield `SingleThreaded`. In `leiden/crates/leiden/src/orchestrator/result.rs` unit tests (named `runresult_threading_is_single_threaded_for_all_termination_reasons`).
- [ ] T047d [P] [T] [US1] Failing test asserting FR-004 `RunResult.seed` round-trip across **every** `TerminationReason` (closes `U2` from `$speckit-analyze`): for each of the three `TerminationReason` variants (`Converged` on `fixtures/two_cliques.edg`, `DegenerateInput` on `fixtures/empty.edg`, `IterationCap` on `fixtures/path.edg` with `iteration_cap = 2`), build a `LeidenParameters { seed: Some(<unique sentinel per case, e.g. 42 / 0xDEAD / 0xBEEF>) , … }`, run the algorithm, and assert `result.seed == params.seed` verbatim. The library `None` default and the `Some(0)` CLI-default round-trip paths are already covered by T066; this task closes the per-`TerminationReason` coverage gap. In `leiden/crates/leiden/src/orchestrator/result.rs` unit tests (named `runresult_seed_round_trips_under_every_termination_reason`).
- [ ] T048 [V] [US1] Verify all US1 tests pass: `cargo test -p leiden two_cliques_yields_two_communities single_node_returns_one_community empty_graph_returns_empty_partition fixture_suite_matches_reference` (depends on T032–T035, T046, **T047d** — A9 remediation: T047d's per-`TerminationReason` `RunResult.seed` round-trip is a contract lock that must be green before US1 verification closes; the bare failing-test marker `[T]` on T047d is insufficient without an explicit `[V]` consumer.)
- [ ] T049 [V] [US1] Verify acceptance scenarios from `spec.md US-1` by running `cargo test -p leiden` and inspecting `cargo doc --workspace --no-deps` output

**Checkpoint**: At this point, User Story 1 is fully functional. Library `Leiden::run` produces well-connected partitions on all US1 fixtures. **MVP deliverable.**

---

## Phase 4: User Story 2 — Tune Resolution and Reproducibility (Priority: P2)

**Goal**: User varies γ and reruns under fixed seed; gets deterministic partition that changes only as γ changes.

**Independent Test**: Run `cargo test -p leiden determinism_under_fixed_seed` (two runs byte-identical) and `resolution_changes_partition` (γ=0.5 vs γ=2.0 differ on karate) per `quickstart.md §4`.

### Tests for User Story 2 (write FIRST, observe FAIL) ⚠️

- [ ] T050 [P] [T] [US2] Failing test: `Leiden::run` with γ ≤ 0 returns `LeidenError::InvalidGamma(gamma)` before any algorithm work in `leiden/crates/leiden/tests/us2_resolution.rs` (named `malformed_invalid_gamma`)
- [ ] T051 [P] [T] [US2] Failing test: `Leiden::run` with `iteration_cap < 1` returns `LeidenError::InvalidIterationCap(cap)` in `leiden/crates/leiden/tests/us2_resolution.rs` (named `invalid_iteration_cap_rejected`)
- [ ] T052 [P] [T] [US2] Failing test: `Leiden::run` on `fixtures/karate.edg` with γ=0.5 and γ=2.0 returns **different** partitions (modularity values within 0.01 of `quickstart.md §4` reference) in `leiden/crates/leiden/tests/us2_resolution.rs` (named `resolution_changes_partition`)
- [ ] T053 [P] [T] [US2] Failing test: `Leiden::run` on `fixtures/karate.edg` with fixed (γ, seed) returns **byte-identical** JSON partition across two consecutive invocations in `leiden/crates/leiden/tests/us2_resolution.rs` (named `determinism_under_fixed_seed`)
- [ ] T054 [P] [T] [US2] Failing test: `Leiden::run` reaches `TerminationReason::Converged` on a trivial 2-clique fixture in fewer than `iteration_cap` iterations in `leiden/crates/leiden/tests/us2_resolution.rs` (named `convergence_before_cap`)
- [ ] T055 [P] [T] [US2] Failing test: `Leiden::run` on `fixtures/path.edg` (line graph, encourages early iteration-cap hit) reaches `TerminationReason::IterationCap` when `iteration_cap = 2` and returns best-so-far partition in `leiden/crates/leiden/tests/us2_resolution.rs` (named `iteration_cap_returns_best_partition`)
- [ ] T055a [P] [T] [US2] Iteration-cap tie-break test (closes `A3` from `$speckit-analyze`): on a fixture constructed so that two distinct partitions at different iterations have identical modularity (within `f64::EPSILON`), assert that the iteration-cap path returns the **earliest-iteration-wins** partition per `spec.md` Edge Case "Iteration cap reached before convergence". In `leiden/crates/leiden/tests/us2_resolution.rs` (named `iteration_cap_tiebreak_prefers_earliest_iteration`).
- [ ] T055b [P] [T] [US2] Iteration-cap community-count tie-break test (closes `A14` from `$speckit-analyze`): on a fixture constructed so that two distinct partitions at the **same iteration** have identical modularity (within `f64::EPSILON`) **AND** identical earliest-iteration-wins status, but different community counts (one strictly more communities than the other), assert that the iteration-cap path returns the **smallest-community-count-wins** partition per `spec.md` Edge Case "Iteration cap reached before convergence" (the second tier of the tie-break chain after earliest-iteration-wins). Companion to T055a which covers only the first tier; together they lock the full tie-break chain per `spec.md` Clarifications 2026-08-30 (the iteration-cap clarification). In `leiden/crates/leiden/tests/us2_resolution.rs` (named `iteration_cap_tiebreak_prefers_smallest_community_count`).
- [ ] T056 [P] [T] [US2] Failing test: Lowest-node-id tie-break in local-moving: a fixture with two equally-good communities yields the lower community id ... (lowest internal `u32` community id, per `spec.md §Edge Cases → Ties in local-moving, refinement, and aggregation`) in `leiden/crates/leiden/src/local_moving/mod.rs` unit tests (named `lowest_id_tiebreak_local_moving`)
- [ ] T057 [P] [T] [US2] Failing test: Lowest-node-id tie-break in refinement: same expectation as T056 for refinement phase ... (lowest internal `u32` community id, per `spec.md §Edge Cases → Ties in local-moving, refinement, and aggregation`) in `leiden/crates/leiden/src/refinement/mod.rs` unit tests (named `lowest_id_tiebreak_refinement`)
- [ ] T057a [P] [T] [US2] Failing test: Lowest-node-id tie-break in aggregation step — fixture with two refined communities having identical aggregate merge weight; assert the lower internal-`u32` community id wins (asserts `spec.md` Edge Cases "Ties in local-moving" extended to aggregation; same lowest-id rule per `research.md §1.5`) in `leiden/crates/leiden/src/aggregation/mod.rs` unit tests (named `lowest_id_tiebreak_aggregation`)

### Implementation for User Story 2

- [ ] T058 [I] [US2] Add γ validation in `Leiden::run` entry: reject `gamma <= 0` and `gamma.is_nan()` with `InvalidGamma` per `FR-003` in `leiden/crates/leiden/src/orchestrator/mod.rs` (depends on T046, T050)
- [ ] T059 [I] [US2] Add `iteration_cap` validation in `Leiden::run` entry: reject `iteration_cap < 1` with `InvalidIterationCap` per `FR-003a` in `leiden/crates/leiden/src/orchestrator/mod.rs` (depends on T051)
- [ ] T060 [I] [US2] Wire γ into `Modularity::delta_move` (already a field; verify the orchestrator passes `params.gamma` per run) in `leiden/crates/leiden/src/quality/modularity.rs`
- [ ] T061 [I] [US2] Implement deterministic tie-break by lowest node id in local-moving (when `delta_q` deltas tie within `f64::EPSILON`, pick lower community id) per `research.md §1.4` and `data-model.md §1.4` in `leiden/crates/leiden/src/local_moving/mod.rs` (depends on T056)
- [ ] T062 [I] [US2] Implement deterministic tie-break by lowest node id in refinement merge predicate per `research.md §1.3` in `leiden/crates/leiden/src/refinement/mod.rs` (depends on T057)
- [ ] T063 [I] [US2] Implement convergence check: `local_moving_changed == false ∧ refined_partition == local_moved_partition` per `research.md §1.3` and `data-model.md §1.9` in `leiden/crates/leiden/src/orchestrator/mod.rs` (depends on T046)
- [ ] T064 [I] [US2] Implement iteration-cap path: track best partition by modularity across iterations; return `TerminationReason::IterationCap` and best partition when cap reached per `FR-003a` and `spec.md §Edge Cases` in `leiden/crates/leiden/src/orchestrator/mod.rs` (depends on T055); per `spec.md §FR-003a` (clarified 2026-08-31) `RunResult.iterations` reports the iteration index at which the algorithm stopped (i.e., the cap reached when `termination_reason == IterationCap`); the iteration at which the best partition was first observed is not separately exposed in v1
- [ ] T065 [V] [US2] Verify all US2 tests pass: `cargo test -p leiden determinism_under_fixed_seed resolution_changes_partition convergence_before_cap iteration_cap_returns_best_partition malformed_invalid_gamma invalid_iteration_cap_rejected` (depends on T052–T057, T058–T064)

**Checkpoint**: User Stories 1 AND 2 are independently functional. Determinism and resolution sensitivity guaranteed.

---

## Phase 5: User Story 3 — Library & CLI Integration (Priority: P3)

**Goal**: Developer integrates the algorithm into another Rust program or shell pipeline via stable library API and CLI binary.

**Independent Test**: `cargo test -p leiden library_api_smoke` (uses `Leiden::new().with_parameters(...).run(&graph)` from `quickstart.md §5.1`) and `cargo test -p leiden-cli cli_round_trip cli_text_format_is_sorted cli_rejects_unknown_format` (`quickstart.md §5.2`).

### Tests for User Story 3 — Library (write FIRST, observe FAIL) ⚠️

- [ ] T066 [P] [T] [US3] Failing test: Library smoke test from `quickstart.md §5.1` (build a `CsrGraph<String>` from `Vec<Edge<String>>`, run with `LeidenParameters::default()`, assert `result.quality.is_finite()` and `result.iterations <= 10`) in `leiden/crates/leiden/tests/us3_library.rs` (named `library_api_smoke`) — also assert `result.seed == params.seed` to lock in the forward-compat round-trip per `spec.md §Clarifications 2026-08-30 (seed field)`
- [ ] T067 [P] [T] [US3] Failing test: Library accepts `CsrGraph<u32>` and `CsrGraph<Uuid>` with the same builder pattern (validates `NodeId: Hash + Eq + Clone + Ord` blanket impl end-to-end; both implement `Ord`) in `leiden/crates/leiden/tests/us3_library.rs` (named `library_accepts_ord_node_id_types`)
- [ ] T066a [P] [T] [US3] FR-006 mutability assertion (closes `U1` from the `$speckit-analyze` report, post robustness remediation 2026-08-31): the **primary** lock is a runtime test in `tests/us3_library.rs` named `leiden_public_api_has_no_mut_methods` that **does not depend on any rustdoc-JSON snapshot** — it directly inspects `Leiden`'s public method set by reflecting over `std::any::type_name` plus a hand-maintained allowlist of the expected public surface (NO new dependency; closes M2 from `$speckit-analyze`), and asserts no public method takes `&mut self` other than the documented builders (`with_parameters`, `with_event_sink`, `with_threads` per FR-012) which consume `self` and return `Self`. The test MUST work and FAIL LOUDLY if a `&mut self` method is added to `Leiden` — it is the primary contract lock and stands alone. **Optional add-on (NOT required for the lock):** the original compile-time `const _: () = ...` sealed-marker block may also be present in `lib.rs` as an additional, optional static check that mirrors the runtime test's intent; either component suffices on its own. The original fragile rustdoc-JSON-snapshot lock has been **removed** as a primary mechanism because its disappearance silently dropped the runtime check; if the JSON snapshot is reintroduced later as an *additive* snapshot test (e.g., for documentation diff review), it MUST NOT replace the runtime test. Locks the FR-006 "without exposing internal mutable state" contract robustly across refactors that drop auxiliary snapshot files.
- [ ] T066b [P] [T] [US3] FR-012 builder compile-test (closes `U1` from `$speckit-analyze`): assert that `Leiden::default().with_threads(NonZeroU32::new(1).unwrap())` (the FR-012 forward-compat builder) compiles and returns `Self` — a pure compiler assertion, no runtime check required. Use a `const _: () = ...` block (mirrors the T066a sealed-marker pattern) that type-checks the builder shape: `let _: fn(Leiden, NonZeroU32) -> Leiden = Leiden::with_threads;`. Companion runtime assertion in `tests/us3_library.rs` named `fr012_with_threads_builder_compiles` calls `Leiden::default().with_threads(NonZeroU32::new(1).unwrap()).run(&graph)` on a tiny 2-node fixture and asserts `result.is_ok()` — locks the FR-012 forward-compat builder and its runtime integration with the orchestrator. NOTE: T129 (post-2026-08-31 A5 remediation) exposes `Leiden::with_threads` unconditionally per FR-012 and gates only the rayon *consumption* of the stored value behind `#[cfg(feature = "parallel")]`. This test asserts the unconditional public-API shape and confirms the resolution; the rayon-consumption feature gate does not affect this test.
- [ ] T066c [P] [T] [US3] CLI-side unset-`--seed` translation test (closes `U2` from `$speckit-analyze`): run the CLI binary on `fixtures/two_cliques.edg` **without** any `--seed` flag, capture stdout, parse the JSON output, and assert `seed == 0` (the integer literal `0`, per `spec.md` FR-004 + `cli-schema.md §1.2`). This is independent of T066's `result.seed == params.seed` round-trip assertion (which exercises the library path); T066c locks the CLI-side translation rule itself. Test name in `leiden/crates/leiden-cli/tests/cli_seed_default.rs`: `cli_default_seed_is_zero_when_flag_omitted`.

### Implementation for User Story 3 — Library

- [ ] T068 [I] [US3] Implement `Leiden::with_parameters(LeidenParameters)` builder method consuming `self` per `library-api.md §2` in `leiden/crates/leiden/src/orchestrator/mod.rs` (depends on T046, T066)
- [ ] T069a [P] [T] [US3] Failing test: `RunResult::partition` (direct field) contains assignments sorted by `Id` natural ordering across `CsrGraph<String>`, `CsrGraph<u32>`, and `CsrGraph<Uuid>` per `library-api.md §7` in `leiden/crates/leiden/src/orchestrator/result.rs` unit tests
- [ ] T069b [I] [US3] Sort `RunResult::partition` by `Id` natural ordering at construction time (per `library-api.md §7`) in `leiden/crates/leiden/src/orchestrator/result.rs` (depends on T047b, T069a)

### Tests for User Story 3 — CLI (write FIRST, observe FAIL) ⚠️

- [ ] T070 [P] [T] [US3] Failing test: CLI parses `--gamma <F>`, `--seed <U>`, `--iteration-cap <N>`, `--format <FMT>`, `--log-file <PATH>`, `--log-level <LVL>` per `cli-schema.md §1.2` in `leiden/crates/leiden-cli/src/parse/args.rs`
- [ ] T071 [P] [T] [US3] Failing test: Edge-list parser accepts tab and comma separators per `cli-schema.md §1.3.1` in `leiden/crates/leiden-cli/src/parse/edge_list.rs`
- [ ] T072 [P] [T] [US3] Failing test: Edge-list parser rejects `# nodes=N` header mismatch (declared count ≠ actual unique nodes) in `leiden/crates/leiden-cli/src/parse/edge_list.rs`
- [ ] T073 [P] [T] [US3] Failing test: JSON adjacency parser accepts `{"nodes": [...], "edges": [[<src>, <dst>], ...], "weights": [<f64>, ...]}` per `cli-schema.md §1.3.2` and rejects a payload whose `edges` and `weights` array lengths disagree in `leiden/crates/leiden-cli/src/parse/json_input.rs`
- [ ] T074 [P] [T] [US3] Failing test: Format dispatch per `cli-schema.md §1.3` precedence:
  (1) extension `.json` → JSON parser; (2) first non-whitespace byte `{` → JSON parser;
  (3) otherwise → edge-list parser; in `leiden/crates/leiden-cli/src/parse/dispatch.rs`
  (named `dispatch_precedence_matches_cli_schema`)
- [ ] T074a [P] [T] [US3] Failing test: Format dispatch tie-break — feed a fixture whose
  first non-whitespace byte is `{` AND extension is `.edg`; assert JSON parser is used
  (extension wins over byte sniff) per `cli-schema.md §1.3` dispatch precedence in
  `leiden/crates/leiden-cli/src/parse/dispatch.rs` (named `dispatch_extension_wins_over_byte_sniff`)
- [ ] T075 [P] [T] [US3] Failing test: JSON output serializes `gamma`, `seed`, `iterations`, `termination_reason`, `quality`, `threading`, `assignments` sorted by node per `cli-schema.md §1.4.1` and the complete field list in `spec.md` FR-007b (post-A18 remediation: `threading` MUST appear as `"SingleThreaded"` in v1) in `leiden/crates/leiden-cli/src/format/json_output.rs`. **A18 verification companion (closes HIGH A18)**: the `[T]` marker alone does not exercise the post-implementation green state; T095's test filter (`cli_round_trip cli_text_format_is_sorted cli_rejects_unknown_format`) does not include T075's exact test name, so the JSON `threading` assertion risks being implemented but never executed by US3's verification step. Fold T075's test name (`json_output_serializes_threaded_single_threaded`) into T095's `cargo test -p leiden-cli` filter so the assertion runs at US3 verification time. The companion assertion lives in T075 itself (it is the failing test); T095's filter is the verification gate.
- [ ] T075a [P] [T] [US3] `ThreadingPolicy` serde string pin (closes `A30` from `$speckit-analyze`): pure-compiler / runtime assertion that `serde_json::to_string(&ThreadingPolicy::SingleThreaded)` yields the **exact** byte sequence `"SingleThreaded"` (PascalCase, no SCREAMING_SNAKE, no lowercase). Rationale: FR-007b mandates `threading: "SingleThreaded"` in JSON output but the `ThreadingPolicy` enum's serde representation is a *separate* contract — a future contributor renaming the Rust variant (e.g., to `Single_Threaded` or `singleThreaded`) would silently break the CLI JSON contract unless the serde rename attribute or Display impl is also updated. This test pins the **observable JSON serialization** independently of the Rust identifier, so a refactor that touches either side fails the test. In `leiden/crates/leiden/src/orchestrator/result.rs` unit tests (named `thread_policy_serializes_to_single_threaded_string`). Companion assertion to T075 (which pins the field name `"threading"` in the full output struct); T075a pins the **value** of the field at the type level. Both names are referenced by T095's filter so US3 verification exercises them.
- [ ] T076 [P] [T] [US3] Failing test: Text output serializes `<node_id>\t<community_id>` lines sorted by node id per `cli-schema.md §1.4.2` in `leiden/crates/leiden-cli/src/format/text_output.rs`
- [ ] T077 [P] [T] [US3] Failing test: Unknown `--format` value rejected with `CliError::UnsupportedFormat` and exit code 2 per `cli-schema.md §1.4.2, §1.6` in `leiden/crates/leiden-cli/tests/cli_format.rs` (named `cli_rejects_unknown_format`)
- [ ] T078 [P] [T] [US3] Failing test: CLI round-trip: `leiden --format json fixtures/two_cliques.edg > /tmp/out.json` then `jq` assertions on assignment count + uniqueness per `quickstart.md §5.2` in `leiden/crates/leiden-cli/tests/cli_round_trip.rs` (named `cli_round_trip`)
- [ ] T079 [P] [T] [US3] Failing test: Text output is sorted by node id per `quickstart.md §5.2` in `leiden/crates/leiden-cli/tests/cli_round_trip.rs` (named `cli_text_format_is_sorted`)
- [ ] T080 [P] [T] [US3] Failing test: Malformed negative weight → stderr `<path>:<line>: invalid weight \`-1.0\`: must be finite and ≥ 0`, exit code 4 per `spec.md US-3` and `cli-schema.md §1.5` in `leiden/crates/leiden-cli/tests/cli_malformed.rs` (named `malformed_negative_weight`)
- [ ] T081 [P] [T] [US3] Failing test: Malformed self-loop → typed error + exit code 4 in `leiden/crates/leiden-cli/tests/cli_malformed.rs` (named `malformed_self_loop`)
- [ ] T082 [P] [T] [US3] Failing test: Malformed dangling node id → typed error + exit code 4 in `leiden/crates/leiden-cli/tests/cli_malformed.rs` (named `malformed_dangling_node`)
- [ ] T083 [P] [T] [US3] Failing test: Malformed γ ≤ 0 → `LeidenError::InvalidGamma` propagates to CLI exit code 3 per `cli-schema.md §1.6` in `leiden/crates/leiden-cli/tests/cli_malformed.rs` (named `malformed_invalid_gamma`)
- [ ] T083a [P] [T] [US3] Failing test: I/O error path → CLI exits with code 5 (closes the I/O acceptance gap introduced as US-3 acceptance scenario 3): run the CLI against a path that does not exist (`fixtures/__does_not_exist__.edg`), against a directory passed as `<GRAPH_FILE>`, AND against a fixture file made unreadable via `std::fs::set_permissions(PERMISSION_DENIED)` (Unix `chmod 000`, gated by `#[cfg(unix)]`); assert exit code 5 and a structured stderr message naming the path + I/O cause; no panic trace. **A13 precondition (root-bypass guard)**: the `chmod 000` branch MUST be skipped when running as uid 0 (root bypasses POSIX permission checks on Linux, so `std::fs::set_permissions(0o000)` on a root-owned test fixture is silently readable and the test gives a false-pass signal); gate the branch on a runtime `unsafe { libc::getuid() } != 0` check (or a Rust-portable equivalent such as reading `/proc/self/status` on Linux) and emit a `tracing::warn!` skip notice so CI logs record the skip; the missing-path and directory-as-file branches do NOT require uid 0 gating and remain unconditional. In `leiden/crates/leiden-cli/tests/cli_io_errors.rs` (named `io_error_yields_exit_code_5`). Platform note (closes G3 from `$speckit-analyze`): the `chmod 000` permission-denied branch is gated by `#[cfg(unix)]`; Windows has no POSIX permission model and the equivalent branch is documented as a no-op test under `#[cfg(not(unix))]` in the same file (named `io_error_unix_only_no_op_on_windows`) to make the platform asymmetry explicit in the test suite. **Exact stderr format pin (closes A29 from `$speckit-analyze`)**: the test MUST additionally assert the stderr line matches the regex `^io: <path>: <cause>$` for the missing-path case (and `^io: <path>: <cause>$` for the permission-denied and directory cases, with `<path>` and `<cause>` captured as groups for diagnostic assertions). This pins the canonical format shown in `cli-schema.md §1.5.1` (the I/O error block: `io: fixtures/__missing__.edg: No such file or directory (os error 2)`) — implementers who deviate from this exact format will fail this test, satisfying FR-007's "structured error message to stderr" requirement at the byte level.

### Implementation for User Story 3 — CLI

- [ ] T084a [P] [T] [US3] Failing test that establishes the canonical CLI stderr line schema before any CLI error code is written — locks the `cli-schema.md §1.5` regex at write time so implementers have an executable spec (closes U1 from `$speckit-analyze`): in `leiden/crates/leiden-cli/tests/cli_stderr_format.rs` (named `cli_stderr_matches_cli_schema_1_5_spec`), assert that the regex `^(loaded graph|iteration <N>|terminated after <N> iterations|<error-class>: <message>)$` matches every line in the canonical examples in `cli-schema.md §1.5` (the test should be runnable against a fixture text block containing one line of each type). This task is intentionally a pure-spec-regex test that fails until `cli-schema.md §1.5` includes the example lines (now present as `§1.5.1`); the runtime counterpart is T094a (`cli_stderr_matches_cli_schema_1_5_at_runtime`) which lives in the same file.
- [ ] T084 [I] [US3] Implement `CliError` thiserror enum per `data-model.md §3.3` in `leiden/crates/leiden-cli/src/error.rs` (depends on T017)
- [ ] T085 [I] [US3] Implement `clap` derive `Args` struct with all flags from `cli-schema.md §1.2` in `leiden/crates/leiden-cli/src/parse/args.rs` (depends on T070)
- [ ] T086 [I] [US3] Implement edge-list parser (tab/comma auto-detect, comment lines, empty lines, error on negative/NaN/self-loop with line number) per `cli-schema.md §1.3.1` in `leiden/crates/leiden-cli/src/parse/edge_list.rs` (depends on T071–T072, T084). The CLI parser MUST validate self-loops BEFORE invoking `CsrGraph::from_edges` so that the offending line number is preserved (`spec.md` FR-008 + `cli-schema.md §1.5` `SelfLoop { line: Some(N), node }` payload contract). The library's `CsrGraph::from_edges` is a second-line defense that emits `SelfLoop { line: None, node }` per T024a.
- [ ] T087 [I] [US3] Implement JSON adjacency parser per `cli-schema.md §1.3.2` in `leiden/crates/leiden-cli/src/parse/json_input.rs` (depends on T073, T084)
- [ ] T088 [I] [US3] Implement format dispatch (extension hint + byte sniff) per `research.md §3.3` in `leiden/crates/leiden-cli/src/parse/dispatch.rs` (depends on T074)
- [ ] T089 [I] [US3] Implement `PartitionOutput` and `Assignment` JSON serialize structs per `data-model.md §3.2` in `leiden/crates/leiden-cli/src/format/json_output.rs` (depends on T075)
- [ ] T090 [I] [US3] Implement text output writer (tab-separated, sorted by node id) per `cli-schema.md §1.4.2` in `leiden/crates/leiden-cli/src/format/text_output.rs` (depends on T076)
- [ ] T091 [I] [US3] Implement CLI `main`: parse args → load graph via format dispatch → run algorithm → serialize output via format → set exit code per `cli-schema.md §1.6` in `leiden/crates/leiden-cli/src/main.rs` (depends on T084–T090)
- [ ] T091a [V] [US3] Verify exit-code table consistency: confirm `cli-schema.md §1.6` exit codes (0 success, 2 unsupported format, 3 invalid γ/cap, 4 malformed input, 5 I/O) are exercised by at least one test each — T077 (exit 2), T080–T082 (exit 4), T083 (exit 3), **T083a (exit 5)**, plus a panic-trace grep that scans all CLI test stderr output for the substrings `"panicked at"` and `"thread 'main'"` and asserts zero hits (closes `M1`) — in `leiden/crates/leiden-cli/tests/cli_exit_codes.rs` (named `all_exit_codes_exercised`)
- [ ] T092 [I] [US3] Initialize `tracing_subscriber` with `EnvFilter` from `RUST_LOG` or `--log-level` per `tui-events.md §4` and `cli-schema.md §1.5` in `leiden/crates/leiden-cli/src/main.rs`
- [ ] T093 [I] [US3] Emit CLI progress lines to stderr (`loaded graph`, `iteration <i>`, `terminated after <k> iterations`) via `tracing` macros per `cli-schema.md §1.5` and `Constitution §VI` (no `eprintln!`) in `leiden/crates/leiden-cli/src/main.rs` (depends on T092)
- [ ] T094 [I] [US3] Implement `--log-file <PATH>` flag handler: writer to file via `tracing_subscriber::fmt::layer().with_writer(file)` per `tui-events.md §2.3` in `leiden/crates/leiden-cli/src/main.rs` (depends on T092)
- [ ] T094a [P] [T] [US3] Failing test asserting CLI stderr line format at runtime (closes `M2` from `$speckit-analyze`): run the CLI on a valid fixture with `--log-level info` and capture stderr; assert each line matches the schema in `contracts/cli-schema.md §1.5` (regex per line: `^(loaded graph|iteration <N>|terminated after <N> iterations|<error-class>: <message>)`); assert no line contains a panic trace substring (`"panicked at"`, `"thread 'main'"`); assert progress lines (non-error) are emitted to stderr and never to stdout. Companion to T084a (which locks the regex at write time as a pure-spec-regex test); this task runs the actual CLI binary. Test name in `leiden/crates/leiden-cli/tests/cli_stderr_format.rs`: `cli_stderr_matches_cli_schema_1_5_at_runtime` (T084a uses the prefix `cli_stderr_matches_cli_schema_1_5` for the spec-regex test — both tests live in the same file, distinct test names).
- [ ] T095 [V] [US3] Verify all US3 library + CLI tests pass: `cargo test -p leiden library_api_smoke library_accepts_ord_node_id_types` then `cargo test -p leiden-cli cli_round_trip cli_text_format_is_sorted cli_rejects_unknown_format json_output_serializes_threaded_single_threaded thread_policy_serializes_to_single_threaded_string malformed_negative_weight malformed_self_loop malformed_dangling_node malformed_invalid_gamma` (depends on T066–T067, T077–T083, T068–T094, T094a, **T075's JSON `threading` field assertion + T075a `ThreadingPolicy` serde string pin** — A18 + A30 remediation: T075's exact test name and T075a's serialization test MUST be in this filter so FR-007b's `threading: "SingleThreaded"` JSON contract is exercised at US3 verification time, not just asserted at write time)
- [ ] T095a [P] [T] [US3] Library-level panic-trace grep (closes `M1` from `$speckit-analyze`; **verification-gate re-run of the same test introduced in T011a — not a new test**): under `--no-fail-fast`, run `cargo test --workspace` and pipe combined stdout+stderr through `grep -E "panicked at|thread 'main'"`; assert **zero matches** across all test output (Constitution §III + FR-008 panic-trace freedom). This complements T091a's CLI-scoped assertion by covering the library. In `leiden/crates/leiden/tests/workspace_panic_trace_grep.rs` (named `workspace_no_panic_traces_under_any_test`); may be implemented as a shell-driven integration test.
- [ ] T095b [P] [T] [US3] CLI `--quiet` / `--log-level error` suppression test (closes G1 from `$speckit-analyze`): run the CLI on a valid fixture with `--log-level error` (the lowest severity in the FR-007 / `cli-schema.md §1.5` chain) and assert that NO `loaded graph:` / `iteration <N>:` / `terminated after <N> iterations:` lines appear on stderr, but the stdout partition is still emitted correctly (exit code 0). This guards against the inverse risk: that future refactors of the `tracing_subscriber` setup accidentally cause `info!` lines to leak past the configured level. In `leiden/crates/leiden-cli/tests/cli_quiet.rs` (named `cli_quiet_suppresses_progress_lines`).
- [ ] T095c [P] [T] [US3] CLI `--log-level warn` asymmetric-suppression test (closes `G1` from `$speckit-analyze`, second half): positive-direction counterpart to T095b. Run the CLI on `fixtures/two_cliques.edg` once with `--log-level info` and once with `--log-level warn`; assert that (a) the `info!` run contains `loaded graph:` / `iteration <N>:` / `terminated after <N> iterations:` progress lines on stderr, and (b) the `warn!` run contains **none** of those lines on stderr. Stdout partition is emitted and exit code is `0` in both runs. This catches the inverse failure mode of T095b: a future refactor of the `tracing_subscriber` setup that causes `info!` lines to leak past a `warn`-level `EnvFilter`. In `leiden/crates/leiden-cli/tests/cli_quiet.rs` (named `cli_warn_level_suppresses_info_progress_lines`).

**Checkpoint**: User Stories 1, 2, AND 3 are independently functional. Library API + CLI binary both pass integration tests.

---

## Phase 6: TUI Binary (Interactive Ratatui Front-end) — (Priority: P3, parallel with US3)

**Goal**: Interactive partition viewer with community panel, graph panel, log pane, status bar — fed by `LeidenEvent` over `mpsc` channel per `tui-events.md`.

**Independent Test**: `cargo test -p leiden-tui` (snapshot tests on `TestBackend`). All panels render correctly with `insta` snapshots approved.

### Tests for TUI (write FIRST, observe FAIL) ⚠️

- [ ] T096 [P] [T] Failing test: TUI snapshot — `Idle` state renders three panels (community list empty, log pane, status bar) per `cli-schema.md §2.4` in `leiden/crates/leiden-tui/tests/us_tui_idle.rs` (named `idle_renders_three_panels`)
- [ ] T097 [P] [T] Failing test: TUI snapshot — `Running` state with `IterationFinished { iteration: 3, quality: 0.4127 }` in events buffer shows progress bar per `tui-events.md §6` in `leiden/crates/leiden-tui/tests/us_tui_running.rs` (named `running_state_shows_progress_bar`)
- [ ] T098 [P] [T] Failing test: TUI snapshot — `Done { iterations: 3, quality: 0.4127 }` state shows final community list + status per `cli-schema.md §2.5` in `leiden/crates/leiden-tui/tests/us_tui_done.rs` (named `done_state_shows_final_partition`)
- [ ] T099 [P] [T] Failing test: `LogPaneLayer::on_event` formats `[level] target: name field=value` per `tui-events.md §5` in `leiden/crates/leiden-tui/src/logging.rs` unit tests
- [ ] T100 [P] [T] Failing test: Ring buffer eviction at 500 entries (501st overwrites oldest) per `tui-events.md §1, §5` in `leiden/crates/leiden-tui/src/logging.rs` unit tests
- [ ] T101 [P] [T] Failing test: App state transitions (`Idle → Running → Done`, `Running → Error`, `Done → Running`, `Error → Idle`) per `cli-schema.md §2.5` and `data-model.md §3.4` recovery expectation in `leiden/crates/leiden-tui/src/app.rs` unit tests
- [ ] T101a [P] [T] Failing test: `Error → Idle` recovery transition — after `AppState::Error(msg)`, a subsequent key press returns the app to `AppState::Idle` without losing the previous `LogRing` history; satisfies `spec.md FR-008` graceful-recovery and `data-model.md §3.4` recovery note in `leiden/crates/leiden-tui/src/app.rs` unit tests (named `error_to_idle_recovery_preserves_log`)

### Implementation for TUI

- [ ] T102 [I] Implement `LogRing` ring-buffer type (500-entry capacity, FIFO eviction) per `tui-events.md §1, §5` in `leiden/crates/leiden-tui/src/logging.rs` (depends on T100)
- [ ] T103 [I] Implement `LogPaneLayer<S>` custom `tracing_subscriber::Layer` per `tui-events.md §5` (formats events, pushes to shared `Arc<Mutex<LogRing>>`) in `leiden/crates/leiden-tui/src/logging.rs` (depends on T099, T102)
- [ ] T104 [I] Implement `App` struct + `AppState` enum (`Idle`, `Running`, `Done`, `Error`) per `data-model.md §3.4` and `cli-schema.md §2.5` in `leiden/crates/leiden-tui/src/app.rs` (depends on T101)
- [ ] T105 [I] Implement key-binding handler (`q`/`Ctrl+C` quit, `r` restart, `s` step, `p` pause, `g` toggle graph, `l` toggle log, `Tab` focus, `?` help) per `cli-schema.md §2.3` in `leiden/crates/leiden-tui/src/event.rs`; every key-binding function and the color-scheme module MUST carry `///` doc comments per `Constitution §IV` and `plan.md §Constraints` (`missing_docs = deny`) in `leiden/crates/leiden-tui/src/event.rs` and `leiden/crates/leiden-tui/src/ui/colors.rs` (T105a)
- [ ] T105a [P] [T] Failing doc-test assertion: every `pub` item in `leiden/crates/leiden-tui/src/event.rs` (key map) and `leiden/crates/leiden-tui/src/ui/colors.rs` (color scheme) carries a `///` doc comment — relies on `cargo doc --workspace --no-deps` failing on `missing_docs = deny` per `Constitution §IV`; assert the build succeeds and `cargo doc` output is clean in `leiden/crates/leiden-tui/tests/us_tui_docs.rs` (named `tui_public_items_documented`)
- [ ] T106 [P] [I] Implement community panel widget (sorted by size desc, columns: id, size, internal-edge weight, total-degree) per `cli-schema.md §2.4` in `leiden/crates/leiden-tui/src/ui/community.rs`
- [ ] T107 [P] [I] Implement graph view widget (BFS-laid-out, communities coloured by hash) per `cli-schema.md §2.4` in `leiden/crates/leiden-tui/src/ui/graph.rs`
- [ ] T108 [P] [I] Implement log pane widget (paragraph over `LogRing`, scroll offset controlled internally) per `cli-schema.md §2.4` and `tui-events.md §5` in `leiden/crates/leiden-tui/src/ui/log_pane.rs`
- [ ] T109 [P] [I] Implement status bar widget (state, iteration, last quality, γ, seed) per `cli-schema.md §2.4` in `leiden/crates/leiden-tui/src/ui/status_bar.rs`
- [ ] T110 [I] Implement `ui::render(frame, &app)` dispatching to all four panel widgets in `leiden/crates/leiden-tui/src/ui/mod.rs` (depends on T106–T109)
- [ ] T111 [I] Implement `worker.rs`: spawn orchestrator on std thread, own `mpsc::Sender<LeidenEvent>`, send events per `tui-events.md §2` in `leiden/crates/leiden-tui/src/worker.rs`
- [ ] T112 [I] Implement drain loop: `while let Ok(event) = rx.try_recv() { app.push(event) }` per `tui-events.md §2` in `leiden/crates/leiden-tui/src/app.rs`
- [ ] T111a [I] [TUI] Wire `Leiden::with_event_sink(mpsc::Sender<LeidenEvent>)` per `library-api.md §6` and `tui-events.md §2` in `leiden/crates/leiden/src/orchestrator/mod.rs` (depends on T124) — **moved from Phase 7 (T125) to satisfy the T113/T114 dependency that the original Phase-7 placement broke**; see `plan.md §Constraints` for the resolution rationale.
- [ ] T111b [P] [T] [TUI] Failing test asserting FR-006/§library-api §6 `Leiden::with_event_sink` builder signature (closes `O1` from `$speckit-analyze`): pure-compiler assertion that the public builder `Leiden::with_event_sink(std::sync::mpsc::Sender<LeidenEvent>) -> Self` exists, taking `self` (consuming) and returning `Self`. Use `const _: () = ...` block with `let _: fn(Leiden, std::sync::mpsc::Sender<leiden::LeidenEvent>) -> Leiden = Leiden::with_event_sink;` — mirrors the T066a sealed-marker pattern. Locks the T111a builder signature at the type level so a future refactor that drops the builder (or changes its signature) is caught at compile time, satisfying Constitution §V Test-First NON-NEGOTIABLE ("every implementation task is preceded by a failing-test task"). In `leiden/crates/leiden/tests/us3_library.rs` (named `with_event_sink_builder_compiles`).
- [ ] T113 [I] Implement bounded channel (1024) with `LeidenEvent::Throttled { dropped: u64 }` fallback per `tui-events.md §2/§3` in `leiden/crates/leiden-tui/src/worker.rs` and `leiden/crates/leiden/src/events.rs` (depends on T019, T111a)
- [ ] T113a [P] [T] [TUI] Failing test: Bounded channel overflow — feed a worker through a channel whose receiver is held but never drained; confirm worker emits `LeidenEvent::Throttled { dropped: u64 }` instead of blocking or panicking; assert `Leiden::run` still returns a `Result` and no panic trace appears on stderr (Constitution §III + `contracts/tui-events.md §2/§3`) in `leiden/crates/leiden-tui/tests/us_tui_backpressure.rs` (named `bounded_channel_overflow_emits_throttled`)
- [ ] T113b [P] [T] [TUI] Failing test: Worker `Sender::send` failure (receiver disconnected mid-run) logs `tracing::warn!` and the worker continues to termination rather than aborting; assert `Leiden::run` returns a `Result` (Constitution §III) in `leiden/crates/leiden-tui/tests/us_tui_backpressure.rs` (named `sender_failure_is_logged_not_fatal`)
- [ ] T113c [P] [T] [TUI] Failing test: Sender-blocked-with-full-channel (closes `M3` from `$speckit-analyze`): construct a 1024-slot bounded channel whose receiver is held but drained only every >100 ms; run the algorithm; assert the orchestrator NEVER blocks for more than 1 ms on a full channel and emits `LeidenEvent::Throttled { dropped: u64 }` instead, satisfying FR-010's "MUST continue to termination rather than block or panic" and `spec.md` Edge Case "Iteration cap reached" performance envelope; assert `Leiden::run` returns a `Result` with finite `quality`. In `leiden/crates/leiden-tui/tests/us_tui_backpressure.rs` (named `full_channel_does_not_block_orchestrator`). Budget rationale (closes M3 from `$speckit-analyze`): the 1 ms per-send assertion is a tight local loop wall-clock, while the >100 ms drain interval is the integration-test pacing; with a factor of >100× between them, transient scheduler jitter on a loaded CI runner cannot bridge the gap, so the per-send wall-clock assertion remains valid. Documented in `quickstart.md §11` (TBD).
- [ ] T113d [P] [T] [US3] **Orchestrator-side `LeidenEvent::Throttled` emission assertion (closes A22 from `$speckit-analyze`)**: failing test that pins the LIBRARY-side behavior (not the worker side, which T113a covers). Construct a `Leiden::default().with_event_sink(slow_tx)` where `slow_tx` is a `std::sync::mpsc::Sender<LeidenEvent>` whose receiver is held but NEVER drained; run `Leiden::run(&small_graph)` on a small fixture (e.g., `fixtures/two_cliques.edg`); assert (a) `Leiden::run` returns `Ok(RunResult)` with `result.quality.is_finite() == true` and `result.termination_reason != TerminationReason::Throttled` (the orchestrator does NOT short-circuit on channel pressure), (b) at least one `LeidenEvent::Throttled { dropped: u64 }` event was emitted (verified via a `tracing_subscriber::fmt::TestWriter` or a side-channel capture of the `with_event_sink` sender's emissions), and (c) `dropped >= 1` (the `u64` counter increments). The test MUST NOT depend on TUI crates; it lives in `leiden/crates/leiden/tests/us3_backpressure.rs` (named `orchestrator_emits_throttled_on_full_sink`). Lock FR-010's "MUST continue to termination rather than block or panic" and the `LeidenEvent::Throttled { dropped: u64 }` back-pressure indicator at the library boundary.
- [ ] T114 [I] Implement TUI `main`: `color_eyre::install → tracing subscriber init (with LogPaneLayer) → ratatui::init → App::run → ratatui::restore` per `tui-events.md §4, §7` in `leiden/crates/leiden-tui/src/main.rs` (depends on T103–T113, T111a)
- [ ] T115 [V] Verify TUI snapshot tests pass: `cargo test -p leiden-tui idle_renders_three_panels running_state_shows_progress_bar done_state_shows_final_partition`; review snapshots via `cargo insta review` (depends on T096–T098, T114)
- [ ] T116 [V] Verify all four key bindings (q/Ctrl+C quit, r restart, g/l toggles, Tab focus) produce expected state transitions in `leiden/crates/leiden-tui/tests/us_tui_keys.rs`

**Checkpoint**: All three user stories AND the TUI binary are functional. `cargo test --workspace` is green; snapshots approved.

---

## Phase 7: Polish & Cross-Cutting Concerns

**Purpose**: Property-based tests, benchmarks, observability integration, CI pipeline, release readiness.

- [ ] T116a [P] [T] SC-002 v1 repeated-run assertion (closes `A12` + `E3` + `F1` from `$speckit-analyze`, post 2026-08-31 SC-002 reconciliation): for **every** fixture in `fixtures/*.expected.json` (the `fixtures/_classification.json` `unique` field is informational in v1 and does NOT gate the test), run `Leiden::run` N=30 times with independent `seed` values at fixed γ and assert that **100%** of runs match the chosen reference partition. The v1 algorithm is deterministic per FR-004 + Clarifications 2026-08-30, so the ≥ 90% clause is out of scope for v1 and the 100% match requirement extends uniformly to all fixtures (unique AND non-unique) — see rewritten `spec.md SC-002`. Fixtures with a unique reference partition under the tie-break remain covered by T035. In `leiden/crates/leiden/tests/us2_sc002_repeated.rs` (named `sc002_v1_all_fixtures_match_in_100_percent_of_runs`).
- [ ] T116b [P] [T] SC-002 fixture cardinality assertion (closes `T2` + `F1` from `$speckit-analyze`): assert that `leiden/fixtures/*.expected.json` contains **at least 10 files**, locking the SC-002 curated-suite guarantee. Walk the `leiden/fixtures/` directory at test time (excluding `_classification.json`), parse each `*.expected.json`, and assert `fixture_count >= 10`. In `leiden/crates/leiden/tests/us2_sc002_cardinality.rs` (named `sc002_curated_suite_has_at_least_10_fixtures`).
- [ ] T117 [P] Implement `proptest` macro for partition invariants: modularity non-decreasing across a local-moving pass in `leiden/crates/leiden/tests/property_modularity.rs` (named `proptest_modularity_non_decreasing`)
- [ ] T118 [P] Implement `proptest` macro: refinement output is a refinement of the input partition (`is_refinement_of == true`) in `leiden/crates/leiden/tests/property_refinement.rs` (named `proptest_refinement_is_refinement_of`)
- [ ] T119 [P] Implement `proptest` macro: across 1000 random weighted graphs (nodes ∈ [10, 1000], edges ∈ [20, 10000]) per `quickstart.md §8` and `spec.md SC-003` — quality is finite, no NaN, no disconnected community in `leiden/crates/leiden/tests/property_nan.rs` (named `proptest_no_nan_no_disconnected`). **A15 explicit depends-on (closes MEDIUM A15)**: SC-003's "without panic" clause has no test of its own — it relies on T011a's workspace-wide `grep -E "panicked at|thread 'main'"` panic-trace gate (Phase 1) and T095a's CLI-scoped panic-trace grep (Phase 5); T119's proptest output MUST additionally be covered by T011a's gate. Add `(depends on T011a)` to this task's metadata so the panic-trace freedom of proptest output is enforced alongside the proptest run, formally closing the "without panic" half of SC-003 inside T119's coverage scope rather than relying on transitive coverage. **The test MUST be configured with `PROPTEST_CASES=1000`** via `leiden/proptest.toml` (`cases = 1000`) so SC-003's "1000 random graphs" contract is honored — `proptest`'s default of 256 cases is insufficient. Set `PROPTEST_MAX_SHRINK_ITERS = 200` in `leiden/proptest.toml` to bound CI cost per failing case; document the bound in `quickstart.md §8` alongside `PROPTEST_CASES=1000`. **SC-003 coverage scope note (closes A23 from `$speckit-analyze`)**: SC-003 mandates "without panic, without NaN/±∞ in computed quality values, AND without producing a non-internally-connected community, in 100% of cases." T119 currently asserts finiteness + no-NaN; the "no disconnected community" assertion is delegated to T120 (`proptest_communities_connected`). Together T119 ∪ T120 satisfy SC-003 end-to-end, but no single test asserts all three properties together. This is intentional (separation of concerns: finiteness vs structural), and the coverage is complete as a union; consumers reviewing SC-003 satisfaction MUST check both T119 and T120.
- [ ] T119a [P] [T] Pathological weight-ratio stress test (closes `E2` from `$speckit-analyze`): construct a small graph with two adjacent K3 cliques where one edge has weight `f64::MIN_POSITIVE` (`~2.2e-308`) and a parallel edge on the other side of the bridge has weight `f64::MAX` (`~1.8e308`); assert `Leiden::run` completes without panic, returns a `Result` with `quality.is_finite() == true`, and every community is internally connected. Covers `spec.md` Edge Case "Numerical extremes" — pathological weight ratios must not produce NaN/±∞. In `leiden/crates/leiden/tests/us1_weight_extremes.rs` (named `pathological_weight_ratios_no_nan`).
- [ ] T120 [P] Implement `proptest` macro: every returned community is internally connected per `FR-002` and `quickstart.md §8` in `leiden/crates/leiden/tests/property_connected.rs` (named `proptest_communities_connected`)
- [ ] T121 [P] Add `criterion` benchmark: local-moving inner loop on `fixtures/lfr_small.edg` (100 nodes, 500 edges) with CI budget assertion < 2 s per `quickstart.md §7` in `leiden/crates/leiden/benches/local_moving.rs`. Track regressions via criterion's `--save-baseline main` mechanism; a nightly CI job may be added to surface drift (out of strict v1 scope; flag for the maintainer).
- [ ] T121a [P] [T] Performance test: `Leiden::run(fixtures/lfr_small.edg)` (100 nodes, 500 edges) completes in `< Duration::from_secs(5)` measured with `std::time::Instant`, single-threaded, gated by `#[cfg(not(debug_assertions))]` so it runs in release only (asserts `spec.md SC-001` performance budget) in `leiden/crates/leiden/tests/perf_sc001.rs` (named `sc001_under_5s_on_lfr_small_fixture`). **Release-only gating rationale (closes A16 from `$speckit-analyze`)**: this test is gated `#[cfg(not(debug_assertions))]` because `cargo test --workspace` (debug build) is the primary correctness gate per Constitution §Development Workflow, while `cargo test --workspace --release` is the additional perf-budget gate added by the `--release` test gate clause (`plan.md §Constraints` "Performance contracts … MUST be exercised under `cargo test --workspace --release` so that `#[cfg(not(debug_assertions))]`-gated perf tests actually execute"). This does NOT violate Constitution §V "every public function or trait method MUST have at least one unit test" because the test exists (just behind a release-only cfg gate) and is exercised by the explicit `--release` invocation in `quickstart.md §1` / T139.
- [ ] T121c [P] [T] Compile-time guard that the `parallel` Cargo feature is OFF by default (closes G2 from `$speckit-analyze`): pure-compiler assertion that protects SC-001's "single modern CPU thread" budget from a future contributor silently enabling parallelism. Implementation: a `const _: () = ...` block in `leiden/crates/leiden/src/lib.rs` (or a `compile_fail` doctest in `leiden/crates/leiden/tests/safety_guards.rs` named `parallel_feature_is_off_by_default`) that asserts `cfg!(feature = "parallel") == false` for the default build. The test MUST be referenced by the `--release` invocation in T139 so it is exercised at pre-merge gate time. Rationale: if a future PR flips `parallel` to default-on under a Constitution MAJOR amendment, this test fails immediately and forces the author to update both the test and SC-001's documentation in the same change.
- [ ] T121b [P] [T] Determinism-under-perf-fixture test: run `Leiden::run(fixtures/lfr_small.edg)` twice consecutively with the same `(γ=1.0, seed=0)` and assert the two `RunResult::partition` `Vec`s are **byte-equal** (sorted, identical community ids per node) and that `quality`, `iterations`, and `termination_reason` are equal — locks the determinism clause of `spec.md SC-001` end-to-end on the contract fixture in `leiden/crates/leiden/tests/perf_sc001.rs` (named `sc001_byte_identical_on_lfr_small_fixture`), gated by `#[cfg(not(debug_assertions))]`. **Release-only gating rationale**: same as T121a above — release-only cfg gate is intentional per Constitution §Development Workflow `--release` test gate clause; this test exists (just behind the cfg) and is exercised by T139's `cargo test --workspace --release` invocation.
- [ ] T122 [P] Add `criterion` benchmark: refinement merge predicate per `research.md §1.3` in `leiden/crates/leiden/benches/refinement.rs`. Track regressions via criterion's `--save-baseline main` mechanism; a nightly CI job may be added to surface drift (out of strict v1 scope; flag for the maintainer).
- [ ] T123 [P] Add `criterion` benchmark: aggregation step per `research.md §1.5` in `leiden/crates/leiden/benches/aggregation.rs`. Track regressions via criterion's `--save-baseline main` mechanism; a nightly CI job may be added to surface drift (out of strict v1 scope; flag for the maintainer).
- [ ] T124 Emit `LeidenEvent::IterationStarted`, `LocalMovingProgress`, `LocalMovingDelta`, `RefinementMerged`, `Aggregation`, `QualityComputed`, `IterationFinished`, `Terminated` via `tracing::info!` with structured fields per `FR-010` in `leiden/crates/leiden/src/orchestrator/mod.rs` and phase modules (depends on T046)
- [ ] T126 Add integration test verifying `tracing` events are emitted with the right structured fields per `specs/001-leiden-algorithm/checklists/observability.md` CHK073–CHK087 (uses `tracing-test` or `tracing_subscriber::fmt::TestWriter`) in `leiden/crates/leiden/tests/observability.rs`
- [ ] T126a [P] [T] Checklist↔`LeidenEvent` 1-to-1 mapping test (closes `U3` from `$speckit-analyze`): assert that every `LeidenEvent` variant enumerated in `data-model.md §1.8` has a corresponding CHK row in `specs/001-leiden-algorithm/checklists/observability.md` (rows CHK073–CHK087 are the placeholder range from T019a). Implementation: open `checklists/observability.md`, parse the CHK-row table, extract the `Variant` column; cross-check against the `LeidenEvent` enum source in `leiden/crates/leiden/src/events.rs` (parse `enum LeidenEvent { ... }` and collect variant names via a build-time `inventory`-style scan OR a `compile_fail` doctest that fails if the variant list and the CHK list diverge). The test MUST fail loudly when a variant is added without a CHK row, preventing silent observability-coverage gaps. In `leiden/crates/leiden/tests/observability_checklist.rs` (named `leidenevent_variant_count_matches_observability_checklist`).
- [ ] T127 Add `cargo deny` configuration (advisories, licenses, sources) per `Constitution §VII` in `leiden/deny.toml`
- [ ] T128 Add CI workflow (GitHub Actions): `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`, `cargo doc --workspace --no-deps`, `cargo deny check` per `Constitution §Development Workflow` and `quickstart.md §1` in `.github/workflows/ci.yml`. Note: branch-protection configuration (`require_pull_request_reviews`, `required_reviewers ≥ 1`) is a maintainer-side GitHub setting, not a code change; document it in `quickstart.md §10` (new section) or `leiden/README.md` per Constitution §Review.
- [ ] T129 [P] Add `--threads N` CLI flag and `Leiden::with_threads(NonZeroU32) -> Self` builder method. **The builder method is unconditional** (no `#[cfg(...)]` gate on the method itself) per `spec.md` FR-012 ("the library MUST expose `Leiden::with_threads(NonZeroU32)`") and is asserted by T066b. The builder stores the thread count on the `Leiden` struct but **does NOT influence v1 execution** — `RunResult.threading` is always `ThreadingPolicy::SingleThreaded` in v1 per FR-012, and the stored value is consumed by a future rayon integration (out of scope for v1). The actual multi-threaded execution path (rayon consumption, parallel iterators) — when/if added — is gated behind `#[cfg(feature = "parallel")]` with `parallel = []` declared in `leiden/crates/leiden/Cargo.toml` and `leiden/crates/leiden-cli/Cargo.toml`. The feature flag MUST be off by default to keep SC-001's single-thread budget valid; default build is single-threaded (per `plan.md §Target Platform` and `Constitution §Additional Constraints "Determinism"`). `rayon` is **not** added in v1 even when the feature is enabled — the stub accepts the value but documents that enabling parallelism is out of scope for v1 and requires a constitution amendment. **A17 dormant-stub clarification (closes HIGH A17 prose tension)**: the `parallel = []` Cargo feature is **reserved for a future Constitution MAJOR amendment that authorizes the multi-threaded variant**; in v1 it is declared only to document the future activation contract — it is NOT a "stub that contributors are free to flip on." A future contributor reading T129 alone MUST NOT interpret `parallel = []` as a dormant slot inviting opt-in consumption; consuming it (default-on, dependencies-clause addition, or `cfg(feature = "parallel")`-gated rayon import) requires (i) a Constitution MAJOR amendment per the Additional Constraints "Stochastic variants" clause and the parallel-execution assumption, AND (ii) updates to T121c (`parallel_feature_is_off_by_default`) and T129a (`rayon_not_in_dependency_tree`) in the same PR. The activation contract (which PR may flip the default to on) is documented in `quickstart.md §11` (TBD — to be added when the multi-threaded variant is greenlit). The activation contract (which PR may flip the default to on) is documented in `quickstart.md §11` (TBD — to be added when the multi-threaded variant is greenlit). **Resolution of T066b's "I3"-noted conflict (closes the T129-vs-spec gap from `$speckit-analyze`, post 2026-08-31):** the builder method is exposed unconditionally per FR-012; the rayon CONSUMPTION of the stored value is gated. `spec.md` US-1 / FR-006 do not mention `--threads N`; `spec.md` Assumption "Parallel execution" (added 2026-08-31) documents this as a deliberate scope decision; if a future spec revision adds multi-threaded execution as a goal, this task's scope must be revisited before the rayon integration.
- [ ] T129a [P] [T] **Cargo.toml-shape guard against accidental rayon addition (closes A17 from `$speckit-analyze`)**: pure-compiler + cargo-tree assertion that `rayon` is NOT in the dependency tree of any workspace crate. Implementation: a `compile_fail` doctest in `leiden/crates/leiden/tests/safety_guards.rs` named `rayon_not_in_dependency_tree` that (a) parses `leiden/Cargo.lock` at test time (or the resolved `cargo metadata --format-version 1` JSON output via `include_str!` on a build-script-generated snapshot) and asserts no `name == "rayon"` package entry exists; (b) a `const _: () = ...` block that type-checks the absence of rayon via a marker: `let _: Option<()> = None::<rayon_marker::Rayon>;` where `pub mod rayon_marker` is `#[cfg(not(any())] mod rayon_marker { pub struct Rayon; }` — i.e., a struct that exists ONLY under `cfg(any())`, which is never true; the type-check therefore succeeds iff rayon is genuinely absent from the dep graph. **Cargo.toml shape requirement** (closes A17): this task MUST produce `leiden/crates/leiden/Cargo.toml` whose `[dependencies]` table does NOT include `rayon = { ..., optional = true }` or any equivalent declaration; the `[features]` table MUST declare `parallel = []` with NO `dependencies = [...]` clause. Any future PR that adds rayon to `[dependencies]` MUST either (i) update this test's `cfg(any())` to include a real cfg target, or (ii) be rejected by `multiple_crate_versions = deny` / `wildcard_dependencies = deny` if it tries to silently re-introduce the dep. The test MUST be referenced by T139's `cargo test --workspace --release` invocation so it runs at pre-merge gate time. Rationale: FR-012 explicitly states "no `rayon` dependency is permitted in the workspace" in v1; a future contributor who adds rayon to the dep tree without consuming it would silently inflate compile times and binary size, and the `parallel` feature's "dormant stub" intent would be defeated. T121c covers the parallel-feature cfg gate; this task covers the underlying dep-tree invariant.
- [ ] T130 [P] Document `Quickstart` walkthrough in `leiden/README.md` matching `quickstart.md §3–§5` (CLI commands, expected outputs)
- [ ] T131 [P] Document library API usage in `leiden/README.md` with the smoke-test snippet from `quickstart.md §5.1`
- [ ] T132 [P] Document TUI usage (`leiden-tui <GRAPH_FILE>`, key bindings) in `leiden/README.md` matching `cli-schema.md §2.3` and `quickstart.md §6`
- [ ] T133 Run `quickstart.md §3` validation: `cargo test -p leiden two_cliques_yields_two_communities single_node_returns_one_community empty_graph_returns_empty_partition fixture_suite_matches_reference` and confirm green
- [ ] T134 Run `quickstart.md §4` validation: `cargo test -p leiden determinism_under_fixed_seed resolution_changes_partition` and confirm green
- [ ] T135 Run `quickstart.md §5` validation: `cargo test -p leiden library_api_smoke` + `cargo test -p leiden-cli cli_round_trip cli_text_format_is_sorted cli_rejects_unknown_format` and confirm green
- [ ] T136 Run `quickstart.md §7` validation: `cargo bench -p leiden --bench local_moving` and confirm < 2 s on `fixtures/lfr_small.edg`
- [ ] T137 Run `quickstart.md §8` validation: `cargo test -p leiden proptest` and confirm 1000-random-graph property tests pass
- [ ] T138 Run `quickstart.md §9` compliance one-liner verification: lints unchanged, public items documented, TDD test commit precedes implementation, no panic-prone patterns (final PR review)
- [ ] T138a [P] [V] FR-009 deviation audit (closes `C2` + `E1` from the `$speckit-analyze` report): grep the workspace for any `// leiden-deviation:` or `// deviation:` markers in `crates/leiden/src/**/*.rs`; for each marker, assert an adjacent citation comment of the form `// ref: Traag 2019 §X.Y` and a corresponding entry in `research.md`. The audit MUST also assert that **no algorithm code in `crates/leiden/src/{local_moving,refinement,aggregation,quality,orchestrator}/**` contains a deviation from the reference algorithm that is uncited (the `quality/` path was added 2026-08-31 to close the C2 finding — `Modularity::total_quality` and `Modularity::delta_move` are Traag 2019 Eq. 1 / A5 implementations and must be covered). Run as a CI test (`leiden/crates/leiden/tests/fr009_deviation_audit.rs`, named `fr009_deviations_are_documented`) gated on the audit script's output. Per Constitution §IX "Domain Accuracy" and FR-009, silent deviations are forbidden. Additionally, every `// ref:` comment MUST match the citation pattern `\bTraag\s+2019\s+§\d+(\.\d+)?\b` (regex), and the adjacent rationale comment MUST be ≥ 20 non-whitespace characters (vacuous citations like `// ref: see paper` are rejected).
- [ ] T139 Run `cargo fmt --workspace && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace && cargo test --workspace --release && cargo doc --workspace --no-deps && cargo deny check` per `quickstart.md §1` and `Constitution §Development Workflow` (final pre-merge gate; the `--release` invocation is required so that T121a and T121b — both gated by `#[cfg(not(debug_assertions))]` — actually execute and assert `spec.md SC-001`)
- [ ] T140 Tag release `v1.0.0` (first stable per `library-api.md §9`) after all checks pass; draft release notes summarising FR-001..FR-010 compliance + constitution compliance line per `Constitution §Compliance review`

**Checkpoint**: Production-ready. All user stories, property tests, benchmarks, observability, CI, and documentation in place. MVP + P2 + P3 fully delivered.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Phase 1 (Setup)**: No dependencies — starts immediately
- **Phase 2 (Foundational)**: Depends on Phase 1 — BLOCKS all user stories
- **Phase 3 (US1 P1)**: Depends on Phase 2
- **Phase 4 (US2 P2)**: Depends on Phase 3 (uses `Leiden::run` and `Partition` from US1)
- **Phase 5 (US3 P3)**: Depends on Phase 3 (library API built on US1 implementation); CLI can begin after `Leiden::run` lands
- **Phase 6 (TUI)**: Depends on Phase 2 + Phase 3 (`LeidenEvent` + `Leiden::run`); can run in parallel with US2 and US3 after Phase 3
- **Phase 7 (Polish)**: Depends on all of US1, US2, US3, TUI

### User Story Dependencies

- **US1 (P1)**: Independent after Foundational — no dependencies on other stories
- **US2 (P2)**: Depends on US1 (`Leiden::run`, `Partition`, `Modularity` must exist)
- **US3 (P3)**: Library API depends on US1; CLI depends on US1 + US2 (`resolution_changes_partition` integration check)
- **TUI**: Depends on US1 (`LeidenEvent`, `Leiden::run`, `Leiden::with_event_sink`); uses `CsrGraph`, `Leiden` from US1

### Within Each User Story

- Tests (`[T]` tasks) MUST be written and FAIL before implementation (`[I]` tasks) per Constitution §V
- Models/types before services (e.g., `CsrGraph` before `Leiden::run`)
- Library before binaries (US3 library tasks T066–T069 before CLI tasks T084–T094)
- Story complete before moving to next priority
- Verification (`[V]` tasks) after implementation completes

### Parallel Opportunities

- All Phase 1 setup tasks marked `[P]` can run in parallel (different files); Phase 1 cross-cutting guards T011a (workspace panic-trace grep) and T011b (FR-009 write-time citation/deviation guard) are also `[P]` and run alongside the other Phase 1 setup tasks
- Phase 2 test tasks T012–T015 are all `[P]` (different files)
- Phase 3 test tasks T022–T035 are largely `[P]` (different files in `tests/` and unit modules)
- Phase 4 test tasks T050–T057 are all `[P]`
- Phase 5 CLI test tasks T070–T083 are mostly `[P]`
- Phase 6 widget implementations T106–T109 are all `[P]` (different files in `ui/`)
- Phase 7 property tests T117–T120 are all `[P]`; benchmarks T121–T123 are all `[P]`; docs T130–T132 are all `[P]`
- After Phase 3, US2, US3-library, US3-CLI, and TUI can proceed in parallel (different subagents can own each)

---

## Parallel Example: User Story 3 — Library + CLI tests in one batch

```bash
# All US3 library + CLI test tasks can land in parallel after US3 implementation begins:
Task: "T066 — library_api_smoke test in crates/leiden/tests/us3_library.rs"
Task: "T067 — library_accepts_ord_node_id_types test in crates/leiden/tests/us3_library.rs"
Task: "T070 — clap Args parsing test in crates/leiden-cli/src/parse/args.rs"
Task: "T071 — edge-list tab/comma parser test in crates/leiden-cli/src/parse/edge_list.rs"
Task: "T073 — JSON adjacency parser test in crates/leiden-cli/src/parse/json_input.rs"
Task: "T075 — JSON output serialization test in crates/leiden-cli/src/format/json_output.rs"
Task: "T076 — text output sorted-by-node test in crates/leiden-cli/src/format/text_output.rs"
Task: "T078 — cli_round_trip integration test in crates/leiden-cli/tests/cli_round_trip.rs"
Task: "T080 — malformed_negative_weight test in crates/leiden-cli/tests/cli_malformed.rs"
```

## Parallel Example: Phase 6 TUI widgets

```bash
# All four widget files are independent and can be implemented in parallel:
Task: "T106 — community panel widget in crates/leiden-tui/src/ui/community.rs"
Task: "T107 — graph view widget in crates/leiden-tui/src/ui/graph.rs"
Task: "T108 — log pane widget in crates/leiden-tui/src/ui/log_pane.rs"
Task: "T109 — status bar widget in crates/leiden-tui/src/ui/status_bar.rs"
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Phase 1: Setup (T001–T011)
2. Complete Phase 2: Foundational (T012–T021)
3. Complete Phase 3: User Story 1 (T022–T049)
4. **STOP and VALIDATE**: `cargo test -p leiden two_cliques_yields_two_communities single_node_returns_one_community empty_graph_returns_empty_partition fixture_suite_matches_reference`
5. **MVP deliverable**: Library produces well-connected partitions on all US1 fixtures. Demo-able.

### Incremental Delivery

1. Phase 1 + Phase 2 → Foundation ready (lints clean, domain types compile)
2. Phase 3 → US1 deliverable (`Leiden::run` works on small graphs) → **MVP demo**
3. Phase 4 → US2 deliverable (resolution sensitivity + determinism) → scientific reproducibility
4. Phase 5 → US3 deliverable (library API + CLI binary) → shell-pipeline integration
5. Phase 6 → TUI deliverable (interactive partition viewer) → operator UX
6. Phase 7 → Polish (proptest, benches, observability, CI, docs) → production-ready
7. Each phase adds value without breaking previous phases

### Parallel Team Strategy (with multiple subagents)

1. Subagent A: Phase 1 + Phase 2 + Phase 3 (foundation + MVP)
2. After Phase 3 completes:
   - Subagent B: Phase 4 (US2 resolution + determinism)
   - Subagent C: Phase 5 (US3 library + CLI)
   - Subagent D: Phase 6 (TUI binary)
3. Subagent E: Phase 7 (polish) after all user stories land

---

## Notes

- `[P]` tasks = different files, no dependencies — safe for parallel execution
- `[Story]` label (US1/US2/US3/TUI) maps each task to its user story for traceability
- `[T]` = write failing test first; `[I]` = implementation; `[V]` = verification (test passes + acceptance check)
- Every user story is independently completable and testable at its checkpoint
- Constitution §V (Test-First NON-NEGOTIABLE): all `[I]` tasks must be preceded by a `[T]` task that demonstrably fails first
- Commit per task or per logical group with Conventional Commits: `feat(leiden-graph):`, `test(leiden-us1):`, `feat(leiden-cli):`, `feat(leiden-tui):`, `chore(workspace):`
- Stop at any checkpoint to validate the story independently
- Avoid: vague tasks (always include file path), same-file conflicts in `[P]` batches, cross-story dependencies that break independence
- The `--threads N` CLI flag and `Leiden::with_threads` builder (T129) are accepted for forward compatibility only and do not consume the flag in v1; see `spec.md` Assumptions "Multi-threaded execution is out of scope for v1"
- Reference `quickstart.md` for runnable validation commands at every checkpoint
- **T125 vacant**: T125 was renumbered to T111a per `plan.md §Constraints` (TUI deadlock dependency). Any future grep for `T125` should resolve to `T111a`.

---

## Phase 8: Convergence

_Appended by `$speckit-converge` after the Phase 1–2 implement pass. Findings trace to spec/plan/tasks; ordered MEDIUM first. Constitution check: no violations (lints, panic-freedom, docs, TDD all verified green at convergence time)._

- [ ] T141 Align `LeidenError::InvalidWeight` value rendering with the canonical `-1.0` format pin: the thiserror interpolation `{value}` in `leiden/crates/leiden/src/error.rs` renders `-1.0` as `` `-1` `` (f64 Display), while `cli-schema.md §1.5.1`, `quickstart.md §5.3`, and T080 pin `` invalid weight `-1.0` ``; switch to `{value:?}` (Debug renders `-1.0`) or explicit formatting, update the T013 Display unit-test expectations, and record the coordinated `data-model.md §1.11` ↔ `cli-schema.md §1.5.1` decision so Phase 5 CLI error output (T080, T084a) matches on first implementation (partial)
- [ ] T142 Create the T019a observability checklist table: append a section to `specs/001-leiden-algorithm/checklists/observability.md` with columns `Variant | Required Fields | Assertion Strategy`, one row per `LeidenEvent` variant from `data-model.md §1.8` (mark `GraphLoaded` informational per the FR-010 A19 remediation — graph-load observability is CLI/TUI-boundary via `tracing::info!`), so T126 can consume per-variant per-field assertions; MUST resolve the CHK-numbering collision with the existing CHK073–CHK150 requirements-quality items in the same file (e.g. distinct section + row ids) without modifying existing checklist markers (partial)
- [ ] T143 Add `uuid` as a dev-dependency of `leiden/crates/leiden/Cargo.toml` and extend the `NodeId` blanket-impl unit tests in `leiden/crates/leiden/src/graph/node_id.rs` to assert `uuid::Uuid` satisfies `NodeId` (`Hash + Eq + Clone + Ord`), closing T012's named-type coverage letter; T067 (Phase 3) needs the same dev-dep end-to-end, so landing it now avoids a Phase 3 manifest change — or, if the maintainer prefers zero new deps, record the documented decision that blanket coverage + T067 satisfies T012 and note it in `research.md` (partial)
- [ ] T144 Review `events::emit_error` in `leiden/crates/leiden/src/events.rs`: it is public API surface absent from the `library-api.md §1` re-export list and from every task; either justify keeping it (add it to `library-api.md §1` via a coordinated contract update + reference from T124/CHK131) or remove it and fold structured error emission into T124's orchestrator call sites (unrequested)
- [ ] T145 Document the cargo#6313 doc-output collision in `leiden/README.md` (or `quickstart.md §1`): `cargo doc --workspace --no-deps` succeeds but the `leiden` lib and the `leiden` bin (leiden-cli) share `doc/leiden/`, so one crate's HTML overwrites the other's and cargo warns; pick and document the CI doc strategy (e.g. per-crate `--output-directory` invocations in T128's workflow) so the Constitution §IV `missing_docs` gate stays auditable (partial)
- [ ] T146 Mark the verified-complete Phase 2 tasks `[x]` in `tasks.md` (T012, T013, T014, T015, T015a, T016, T017, T018, T019, T020, T021 — excluding still-open T019a, tracked above as T142) so the task ledger reflects the implemented foundation; verification evidence at convergence time: `cargo test --workspace` 30 tests green, `cargo clippy --workspace --all-targets -- -D warnings` clean, `cargo doc --workspace --no-deps` builds (partial)