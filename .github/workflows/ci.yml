name: CI

on:
  push:
    branches: [ "main", "dev" ]
  pull_request:
    branches: [ "main", "dev" ]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"

jobs:
  check-and-test:
    name: Code Rigor & Test Suite
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.85.0"
          components: rustfmt, clippy

      - name: Rustfmt Check
        working-directory: leiden
        run: cargo fmt --check

      - name: Clippy Workspace Check
        working-directory: leiden
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Unit & Integration Tests (Debug)
        working-directory: leiden
        run: ct --workspace

      - name: Release Tests (Performance SC-001 & Determinism)
        working-directory: leiden
        run: ct --workspace --release

      - name: Documentation Build
        working-directory: leiden
        run: cargo doc --workspace --no-deps

      - name: Install cargo-deny
        uses: taiki-e/install-action@cargo-deny

      - name: Cargo Deny Check
        working-directory: leiden
        run: cargo deny --config deny.toml check
