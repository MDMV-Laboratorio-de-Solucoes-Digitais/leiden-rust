//! Partition types (stub for Phase 2; real implementation in Phase 3).

// ref: Traag 2019 §3 — partition/mod.rs stub; real Partition (assignment, sigma_in/tot) lands in Phase 3 (US1).

/// Placeholder — real `Partition` lands in Phase 3 (US1).
#[derive(Debug)]
pub struct Partition;
