//! `leiden-tui` interactive binary entry point.
//!
//! Phase 1 stub: binary exists for `cargo check --workspace` to succeed.
//! Real TUI rendering and event loop land in Phase 6 (TUI phase).

const fn main() {}
